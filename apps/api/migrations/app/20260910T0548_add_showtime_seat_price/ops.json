[
  {
    "id": "column.public.showtimeSeat.price",
    "label": "Add column \"price\" to \"showtimeSeat\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "price",
        "table": "showtimeSeat"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"price\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "showtimeSeat",
          "price"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"price\"",
        "sql": "ALTER TABLE \"public\".\"showtimeSeat\" ADD COLUMN \"price\" int4"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"price\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "showtimeSeat",
          "price"
        ]
      }
    ]
  }
]