# SeatLock

SeatLock is a production-style real-time seat reservation and booking platform.

## Project Structure

- `apps/web` - Frontend application
- `apps/api` - Backend API
- `packages` - Shared packages
- `docs` - Project documentation